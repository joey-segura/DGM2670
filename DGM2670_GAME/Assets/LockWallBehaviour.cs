using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LockWallBehaviour : MonoBehaviour
{
    //Four bools, once all are marked true the wall lowers using a LERP.
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
